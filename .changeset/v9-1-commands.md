---
'@thesharks/discord': minor
---

Ported the v8 fun and utility commands: `/8ball`, `/advice`, `/booru` (e621, Rule34, and Derpibooru), `/cat`, `/dice`, `/dog`, `/info`, `/inspire`, `/invite`, `/ping`, and `/urbandictionary`. Command names, descriptions, and cooldown messages are localized, and `/booru e621` switches to the safe-rated e926 outside NSFW channels.
